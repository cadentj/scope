# Web

Follow the instructions [here](https://developers.cloudflare.com/pages/get-started/direct-upload/) to deploy from CLI.

```
pn wrangler login
pn wrangler pages project create
pn wrangler pages deploy <BUILD_OUTPUT_DIRECTORY>
```